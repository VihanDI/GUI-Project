<Window x:Class="GUI_Project.Views.ConfirmDeleteUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI_Project.Views"
        Icon="/Icons/Icon.png"
        mc:Ignorable="d"
        Title="Confirmation" Height="140" Width="280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">

    <Grid>
        <Border CornerRadius="0">
            <Border.Background>
                <SolidColorBrush Color="#ffffff">

                </SolidColorBrush>
            </Border.Background>

            <Grid>
                <StackPanel Orientation="Vertical">
                    <TextBlock Style="{StaticResource normalText}" FontSize="14" Margin="10 20 10 0" Text="Are you sure you delete this user?"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Style="{StaticResource messageBoxSecondaryButton}" Content="No" Click="Button_No"/>
                        <Button Style="{StaticResource messageBoxPrimaryButton}" Content="Yes" Click="Button_Yes"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
