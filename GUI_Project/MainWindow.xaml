<Window x:Class="GUI_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI_Project"
        xmlns:Icons ="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1200">
    <Window.DataContext>
        <local:MainWindowVM/>
    </Window.DataContext>
    <Grid>
        <TabControl Name="Navigation" HorizontalAlignment="Stretch" BorderBrush="Transparent">
            <TabItem Name="Login" Header="Tab1" Visibility="Collapsed">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Style="{StaticResource titleText}" Text="Point of Sale Sysytem"/>

                        <TextBlock Text="Enter Username and Password to Sign In" Style="{StaticResource normalText}" Margin="0 10 0 0"/>

                        <Border Width="450" BorderThickness="1" BorderBrush="Black" CornerRadius="5" Margin="0 20 0 10">
                            <Grid Margin="7 9">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Icons:PackIconMaterial Kind="Account" Foreground="#999999" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="5" Grid.Column="0"/>
                                <TextBox Style="{StaticResource dataEntry}" Grid.Column="1"/>
                            </Grid>
                        </Border>

                        <Border Width="450" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Margin="70 7">
                            <Grid Margin="7 9">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Icons:PackIconMaterial Kind="Lock" Foreground="#999999" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="5" Grid.Column="0"/>
                                <PasswordBox Style="{StaticResource passEntry}" Grid.Column="1"/>
                            </Grid>
                        </Border>

                        <Button Style="{StaticResource mainButton}" Margin="0 30 0 0" Content="Sign In" Click="Button_Sign_In"/>

                        <TextBlock Text="Don't have an account?" Margin="0 40 0 0" Style="{StaticResource normalText}"/>

                        <Button Style="{StaticResource mainButton}" Content="Sign Up"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Name="MainMenu" Header="Tab2" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.25*"/>
                        <RowDefinition Height="1.5*"/>
                    </Grid.RowDefinitions>

                    <Border CornerRadius="15 0 0 15" Grid.Row="0">
                        <Border.Background>
                            <SolidColorBrush Color="#ffffff">

                            </SolidColorBrush>
                        </Border.Background>

                        <Grid>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource circleButton}" Click="Button_Back">
                                        <Icons:PackIconMaterial Kind="ArrowLeft"/>
                                    </Button>
                                    <TextBlock Style="{StaticResource titleText}" TextAlignment="Left" Margin="0 15" Text="Welcome"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border CornerRadius="0 15 15 0" Background="#f6f6f6" Grid.Row="1">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Style="{StaticResource mainMenuButton}" Click="Button_New">
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <Icons:PackIconMaterial Kind="Plus" Height="40" Width="40" HorizontalAlignment="Center" Margin="0 20"/>
                                    <TextBlock Style="{StaticResource normalText}" Foreground="White" FontSize="20" Margin="20" Text="New Transaction"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource mainMenuButton}">
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <Icons:PackIconMaterial Kind="TshirtV" Height="40" Width="40" HorizontalAlignment="Center" Margin="0 20"/>
                                    <TextBlock Style="{StaticResource normalText}" Foreground="White" FontSize="20" Margin="20" Text="Products"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource mainMenuButton}">
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <Icons:PackIconMaterial Kind="History" Height="40" Width="40" HorizontalAlignment="Center" Margin="0 20"/>
                                    <TextBlock Style="{StaticResource normalText}" Foreground="White" FontSize="20" Margin="20" Text="Transaction History"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource mainMenuButton}">
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <Icons:PackIconMaterial Kind="Logout" Height="40" Width="40" HorizontalAlignment="Center" Margin="0 20"/>
                                    <TextBlock Style="{StaticResource normalText}" Foreground="White" FontSize="20" Margin="20" Text="Log Out"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Name="NewTransaction" Header="Tab3" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.25*"/>
                        <RowDefinition Height="1.5*"/>
                    </Grid.RowDefinitions>

                    <Border CornerRadius="15 0 0 15" Grid.Row="0">
                        <Border.Background>
                            <SolidColorBrush Color="#ffffff">

                            </SolidColorBrush>
                        </Border.Background>

                        <Grid>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource circleButton}" Click="Button_Back">
                                        <Icons:PackIconMaterial Kind="ArrowLeft"/>
                                    </Button>
                                    <TextBlock Style="{StaticResource titleText}" TextAlignment="Left" Margin="0 15" Text="New Transaction"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border CornerRadius="0 0 0 0" Background="#f6f6f6" Grid.Row="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2.5*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border CornerRadius="5" Height="570" VerticalAlignment="Top" Margin="20 20 20 0" Grid.Column="0">
                                <ScrollViewer>
                                    <ListView ItemsSource="{Binding Products}" BorderBrush="Transparent" FontFamily="IBM Plex Sans Regular" FontWeight="Thin" Name="List1">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="5" Margin="5" BorderBrush="Black" CornerRadius="5" Height="100">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding ProductName}" VerticalAlignment="Center" Margin="4" Padding="5" FontSize="16"/>
                                                        <TextBlock Text="{Binding Price}" VerticalAlignment="Center" Margin="4" Padding="5" FontSize="16"/>
                                                        <Button Style="{StaticResource listButton}" Margin="450 0 0 0">
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <TextBlock Foreground="White" FontSize="14" Text="Add to Cart" VerticalAlignment="Center" Margin="10 10"/>
                                                                <Icons:PackIconMaterial Kind="Cart" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </ScrollViewer>
                            </Border>

                            <Border CornerRadius="5" Height="570" Background="White" VerticalAlignment="Top" Margin="0 20 20 0" Grid.Column="1">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="Sub Total :" Style="{StaticResource normalText}" HorizontalAlignment="Left" Margin="20"/>
                                    <TextBlock/>
                                    <TextBlock Text="No of Items in the Cart :" Style="{StaticResource normalText}" HorizontalAlignment="Left" Margin="20"/>
                                    <TextBlock/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 320 0 0">
                                        <Button Height="50" Style="{StaticResource primaryButton}" HorizontalAlignment="Right">
                                            <Icons:PackIconMaterial Kind="Cart" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Button>
                                        <Button Content="Check Out" Height="50" Style="{StaticResource primaryButton}" HorizontalAlignment="Right"/>
                                    </StackPanel>

                                    
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Name="Products" Header="Tab3" Visibility="Collapsed">
                <Grid>

                </Grid>
            </TabItem>

            <TabItem Name="History" Header="Tab3" Visibility="Collapsed">
                <Grid>

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
